<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar</title>
    <link rel="stylesheet" href="./bootstrap.css">
</head>
<body>
    <%- include('./partials/navbar'); %>
    <div class="container my-4">
        <form action="/posts/<%= post.id %>" method="post">
            <fieldset>
            <legend>Editar Posteo</legend>
            <div class="form-group">
                <input type="text" class="form-control" name="title"  placeholder="@titulo" value="<%= post.title %>">
            </div>
            <div class="form-group">
                <textarea class="form-control" name="content" id="" cols="30" rows="10" placeholder="@contenido"><%= post.content %></textarea>
                <input type="text" class="form-control" name="image"  placeholder="@url-de-la-Imagen" value="<%= post.image %>">
            </div>            
            <button type="submit" class="btn btn-primary">Editar Post</button>
            </fieldset>
        </form>
    </div>
<script src="./bootstrap.js"></script>
</body>
</html>